<!DOCTYPE html>
<html>
<head>
  <title>Rød/Grønn Rulett</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-red-900 to-green-800 text-white min-h-screen flex flex-col items-center justify-center p-6">
  <h1 class="text-3xl font-bold mb-4">🎯 Rød / Grønn Rulett</h1>
  <p class="mb-2">Saldo: <strong><%= balance %></strong> 🪙</p>
  <% if (message) { %>
    <p class="mb-4 text-xl"><%= message %></p>
  <% } %>
  <form method="POST" class="flex gap-6">
    <input type="hidden" name="userId" value="<%= userId %>">
    <button name="color" value="red" class="bg-red-600 px-6 py-3 rounded-lg font-bold hover:bg-red-700">🔴 Rød</button>
    <button name="color" value="green" class="bg-green-600 px-6 py-3 rounded-lg font-bold hover:bg-green-700">🟢 Grønn</button>
  </form>
</body>
</html>
