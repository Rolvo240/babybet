<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Rød/Grønn Rulett</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Poppins', sans-serif; }
    button:disabled {
        opacity: 0.5;
        cursor: not-allowed;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-red-900 to-green-800 text-white min-h-screen flex flex-col items-center justify-center p-6">
  <div class="absolute top-4 left-4">
    <a href="/casino/<%= userId %>" class="bg-white/10 hover:bg-white/20 p-3 rounded-full transition flex items-center justify-center">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
      </svg>
    </a>
  </div>

  <h1 class="text-3xl font-bold mb-4">🔴🟢 Rød / Grønn Rulett</h1>
  <p class="mb-2">Saldo: <strong><%= balance %></strong> 🪙</p>
  <% if (message) { %>
    <p class="mb-4 text-xl"><%= message %></p>
  <% } %>

  <% const canDoFullMur = balance >= 500; %>

  <form method="POST" class="flex flex-col items-center gap-4">
    <input type="hidden" name="userId" value="<%= userId %>">

    <div class="flex gap-6">
      <button type="submit" name="color" value="red" class="bg-red-600 px-6 py-3 rounded-lg font-bold hover:bg-red-700">🔴 Rød</button>
      <button type="submit" name="color" value="green" class="bg-green-600 px-6 py-3 rounded-lg font-bold hover:bg-green-700">🟢 Grønn</button>
    </div>

    <button type="submit" name="color" value="full_mur"
            class="bg-indigo-600 text-white px-4 py-2 rounded-lg font-bold text-sm hover:bg-indigo-700 w-40 text-center"
            <%= canDoFullMur ? '' : 'disabled' %>>
      💥 Full Mur (500)
    </button>
  </form>
</body>
</html>
