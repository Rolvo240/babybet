<!DOCTYPE html>
<html>
<head>
  <title>Keeg eller ikke Keeg</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-900 to-black text-white min-h-screen flex flex-col items-center justify-center p-6">
  <h1 class="text-3xl font-bold mb-4">🪙 Keeg eller Ikke keeg</h1>
  <p class="mb-2">Saldo: <strong><%= balance %></strong> 🪙</p>
  <% if (message) { %>
    <p class="mb-4 text-xl"><%= message %></p>
  <% } %>
  <form method="POST" class="flex gap-6">
    <input type="hidden" name="userId" value="<%= userId %>">
    <button name="guess" value="keeg" class="bg-yellow-500 px-6 py-3 rounded-lg font-bold hover:bg-yellow-600">👑 Keeg</button>
    <button name="guess" value="ikke" class="bg-gray-600 px-6 py-3 rounded-lg font-bold hover:bg-gray-700">💤 Ikke keeg</button>
  </form>
</body>
</html>
